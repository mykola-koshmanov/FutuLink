<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- The cached test mode and the measurement history are device-local, so nothing is backed up. -->
    <exclude domain="file" path="datastore" />
    <exclude domain="database" path="futulink.db" />
</full-backup-content>
